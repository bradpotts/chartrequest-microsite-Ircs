# Disable package lock to avoid conflicts
package-lock=false

# Use a more stable registry
registry=https://registry.npmjs.org/

# Reduce concurrency to avoid ETXTBSY errors
maxsockets=1

# Force npm to be more patient
timeout=300000
